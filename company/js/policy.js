// Neev Employer Portal — policy configuration save.
function savePolicy(){
  toast('Policy saved. Changes apply immediately to all new loan requests.');
  logAction('Updated policy configuration');
}
